pipeline {
    agent any

    stages {
        stage('Checkout Source') {
            steps {
                echo 'Checking out source code from GitHub'
                checkout scm
            }
        }
    }
}
